import { CreatedTaskResponseModel } from './created-task-response-model';

describe('CreatedTaskResponseModel', () => {
  it('should create an instance', () => {
    expect(new CreatedTaskResponseModel()).toBeTruthy();
  });
});
